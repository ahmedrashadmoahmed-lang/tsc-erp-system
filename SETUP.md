# 🚀 دليل التثبيت الكامل - نظام TSC ERP

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

### 1. Node.js & npm
```bash
# تحقق من النسخة
node --version  # يجب أن تكون 18 أو أحدث
npm --version

# تحميل Node.js من
https://nodejs.org/
```

### 2. PostgreSQL
```bash
# تحقق من النسخة
psql --version  # يجب أن تكون 14 أو أحدث

# تحميل PostgreSQL من
https://www.postgresql.org/download/
```

### 3. Git (اختياري)
```bash
git --version
```

---

## 🛠️ خطوات التثبيت

### الخطوة 1: تحميل المشروع

#### الطريقة 1: من GitHub
```bash
git clone https://github.com/yourusername/tsc-erp-complete.git
cd tsc-erp-complete
```

#### الطريقة 2: من ملف ZIP
1. قم بتحميل الملف
2. فك الضغط
3. افتح Terminal/CMD في المجلد

---

### الخطوة 2: تثبيت المكتبات

```bash
npm install
```

**⏱️ الوقت المتوقع:** 2-3 دقائق

---

### الخطوة 3: إعداد قاعدة البيانات

#### أ) إنشاء قاعدة البيانات في PostgreSQL

```bash
# افتح PostgreSQL
psql -U postgres

# أنشئ قاعدة بيانات جديدة
CREATE DATABASE tsc_erp;

# أنشئ مستخدم (اختياري)
CREATE USER tsc_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE tsc_erp TO tsc_user;

# اخرج
\q
```

#### ب) إعداد ملف .env

```bash
# انسخ ملف المثال
cp .env.example .env
```

افتح ملف `.env` وعدّل:

```env
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/tsc_erp?schema=public"

NEXTAUTH_SECRET="قم-بتوليد-مفتاح-عشوائي-هنا"
NEXTAUTH_URL="http://localhost:3000"

JWT_SECRET="قم-بتوليد-مفتاح-عشوائي-آخر"
```

**💡 نصيحة:** لتوليد مفتاح عشوائي:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

#### ج) إنشاء الجداول

```bash
# إنشاء جميع الجداول (50+ جدول)
npx prisma db push
```

**✅ النتيجة:** يجب أن ترى رسالة:
```
✔ Generated Prisma Client
```

#### د) إضافة بيانات تجريبية (اختياري)

```bash
npx prisma db seed
```

---

### الخطوة 4: تشغيل المشروع

```bash
# وضع التطوير
npm run dev
```

**✅ النجاح:** يجب أن ترى:
```
▲ Next.js 14.x.x
- Local:        http://localhost:3000
```

---

### الخطوة 5: تسجيل الدخول

1. افتح المتصفح على: **http://localhost:3000**
2. ستُحوّل تلقائياً لصفحة تسجيل الدخول

**بيانات الدخول الافتراضية:**
```
اسم المستخدم: admin
كلمة المرور: Admin@123
```

---

## 🔧 إدارة قاعدة البيانات

### فتح Prisma Studio (واجهة بيانات بصرية)

```bash
npx prisma studio
```

سيفتح على: **http://localhost:5555**

### أوامر Prisma المفيدة

```bash
# عرض الجداول
npx prisma db pull

# إعادة إنشاء قاعدة البيانات
npx prisma migrate reset

# إنشاء نسخة احتياطية (PostgreSQL)
pg_dump -U postgres tsc_erp > backup.sql

# استعادة النسخة الاحتياطية
psql -U postgres tsc_erp < backup.sql
```

---

## 🚨 حل المشاكل الشائعة

### المشكلة 1: خطأ في الاتصال بقاعدة البيانات

```
Error: P1001: Can't reach database server
```

**الحل:**
1. تأكد من تشغيل PostgreSQL
2. تحقق من `DATABASE_URL` في `.env`
3. جرّب الاتصال يدوياً:
```bash
psql -U postgres -d tsc_erp
```

### المشكلة 2: خطأ في تثبيت المكتبات

```
npm ERR! code ERESOLVE
```

**الحل:**
```bash
npm install --legacy-peer-deps
```

### المشكلة 3: Port 3000 مشغول

**الحل:**
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :3000
kill -9 <PID>

# أو استخدم port آخر
npm run dev -- -p 3001
```

### المشكلة 4: Prisma Client لم يُنشأ

**الحل:**
```bash
npx prisma generate
```

---

## 🏗️ البناء للإنتاج

### 1. بناء المشروع

```bash
npm run build
```

### 2. تشغيل الإنتاج

```bash
npm start
```

### 3. النشر على Vercel (مجاني)

```bash
# تثبيت Vercel CLI
npm i -g vercel

# النشر
vercel
```

### 4. النشر على خادم خاص

```bash
# بناء المشروع
npm run build

# نقل الملفات إلى السيرفر
# يجب نقل:
# - .next/
# - node_modules/
# - public/
# - prisma/
# - package.json
# - .env

# على السيرفر
npm start
```

---

## 📊 التحقق من التثبيت

### 1. فحص قاعدة البيانات

```bash
npx prisma studio
```

### 2. فحص APIs

```bash
# اختبر API تسجيل الدخول
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"Admin@123"}'
```

### 3. فحص الصفحات

- **الرئيسية:** http://localhost:3000
- **تسجيل الدخول:** http://localhost:3000/login
- **Dashboard:** http://localhost:3000/dashboard

---

## 🎓 الخطوات التالية

بعد التثبيت الناجح:

1. ✅ استكشف Dashboard
2. ✅ أنشئ أول عميل
3. ✅ أنشئ أول فاتورة
4. ✅ راجع التقارير
5. ✅ اقرأ الوثائق الكاملة في `README.md`

---

## 📞 المساعدة

إذا واجهت أي مشكلة:

1. **راجع الأخطاء في Terminal**
2. **تحقق من ملف `.env`**
3. **تأكد من تشغيل PostgreSQL**
4. **راجع قسم حل المشاكل أعلاه**

---

## ✅ Checklist التثبيت

- [ ] Node.js 18+ مثبت
- [ ] PostgreSQL 14+ مثبت
- [ ] `npm install` نجح
- [ ] ملف `.env` تم إعداده
- [ ] `npx prisma db push` نجح
- [ ] `npm run dev` يعمل
- [ ] يمكن فتح http://localhost:3000
- [ ] يمكن تسجيل الدخول

**🎉 مبروك! نظام TSC ERP جاهز للاستخدام!**
